{
    "extends": "solhint:all",
    "plugins": [],
    "rules": {
        "avoid-suicide": "error",
        "avoid-sha3": "warn",
        "compiler-version": ["error", "0.8.15"],
        "func-visibility": ["warn", { "ignoreConstructors": true }]
    }
}

